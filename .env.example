# ===========================================
# SS INTER - Environment Variables Template
# ===========================================
# Issue #9: Environment Variables Migration (2025-08-14)
# Arquivo centralizado para todas as configurações
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Substitua os valores de exemplo pelos seus valores reais
# 3. Nunca commite o arquivo .env (está no .gitignore)

# ===========================================
# 🔴 PRIORIDADE ALTA - Credenciais Supabase
# ===========================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Frontend (manter compatibilidade)
NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# ===========================================
# 🔑 API KEYS E CREDENCIAIS EXTERNAS
# ===========================================
# Shotstack API
SHOTSTACK_API_KEY=your-shotstack-api-key
SHOTSTACK_API_URL=https://api.shotstack.io/v1

# ===========================================
# 🐳 INFRAESTRUTURA E SERVIÇOS
# ===========================================
# Redis Configuration
REDIS_URL=redis://localhost:6379

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
ENVIRONMENT=development

# ===========================================
# ☁️ GOOGLE CLOUD STORAGE CONFIGURATION
# ===========================================
GCS_BUCKET=your-gcs-bucket-name
GCS_PATH_PREFIX=videos
GCS_ACL=publicRead
# Path to your GCP service account credentials file
GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-credentials.json

# ===========================================
# ⚙️ WORKER SCALING CONFIGURATION
# ===========================================
# Configure para diferentes cargas de trabalho:
# NORMAL: 50 jobs (padrão)
# HIGH_LOAD: 80 jobs (worker-1 + worker-2) 
# ULTRA_HIGH_LOAD: 180 jobs (worker-1 + worker-2 + worker-3)

# Worker Configuration (Customizable)
ARQ_MAX_JOBS=50
ARQ_JOB_TIMEOUT=600
ARQ_KEEP_RESULT=7200

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600

# ===========================================
# 🟡 PRIORIDADE MÉDIA - Configurações de Cron
# ===========================================
# Configuração de sincronização de expiração (formato cron hour)
EXPIRATION_SYNC_CRON_HOURS=*/6
# Hora do cleanup diário (0-23)
CLEANUP_JOB_CRON_HOUR=3

# ===========================================
# 🟡 PRIORIDADE MÉDIA - Políticas de Retenção
# ===========================================
# Tempo de retenção de vídeos (em dias) - deve coincidir com política GCS
VIDEO_RETENTION_DAYS=2
# Tempo para limpeza de registros antigos no banco (em dias)
DB_RECORD_CLEANUP_DAYS=30

# ===========================================
# 🟡 PRIORIDADE MÉDIA - Valores Padrão Frontend
# ===========================================
# Saldo de tokens inicial/fallback para novos usuários
NEXT_PUBLIC_DEFAULT_TOKEN_BALANCE=100
# Tempo de retenção em horas para UI (48h = 2 dias)
NEXT_PUBLIC_VIDEO_RETENTION_HOURS=48
# Limite de vídeos recentes exibidos no dashboard
NEXT_PUBLIC_RECENT_VIDEOS_LIMIT=10
# Limite de renders para cálculo de tempo médio
NEXT_PUBLIC_AVG_RENDER_TIME_LIMIT=10

# ===========================================
# 🟢 PRIORIDADE BAIXA - Timeouts e Limites de API
# ===========================================
# Timeout para chamadas à API do Shotstack (em segundos)
SHOTSTACK_API_TIMEOUT_SECONDS=10.0
# Timeout para verificação de arquivos no GCS (em segundos)
GCS_HEAD_REQUEST_TIMEOUT_SECONDS=5.0
# Limite de vídeos por página na API de usuário
USER_VIDEOS_PAGE_LIMIT=20

# ===========================================
# 🎨 FRONTEND CONFIGURATION (OPCIONAL)
# ===========================================
# Node environment
NODE_ENV=production

# ===========================================
# 💳 STRIPE CONFIGURATION (FUTURO)
# ===========================================
# Stripe (quando implementar compra de tokens)
# STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
# STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# ===========================================
# 🔧 INTERNAL SERVICES (FUTURO)
# ===========================================
# Internal API communication (quando implementar)
# INTERNAL_API_KEY=your-internal-api-key
# INTERMEDIARY_SERVICE_URL=http://localhost:8001
# WEB_SERVICE_URL=http://localhost:3000